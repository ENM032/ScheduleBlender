@* Autogenerated by Visual Studio *@

@using Microsoft.AspNetCore.Identity
@using ScheduleBlenderWebApp.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@* 
To get the username to display, the code was taken from a YouTube Video
Titled: Display FirstName instead of UserName after Login in Identity - ASP.NET Core 6
Uploaded by: Coding World
Available at: https://www.youtube.com/watch?v=QTns5a_R5S0 
*@
<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
            <a id="manage" class="nav-link text-dark">Hello @UserManager.GetUserAsync(User).Result.Displayname!</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Logout</button>
        </form>
    </li>
}
</ul>
